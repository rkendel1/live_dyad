<script lang="ts">
  import { defineSystemEmbed } from '@stacklive/embed-sdk';

  // Props
{{#each props}}
  export let {{name}}: {{type}}{{#if default}} = {{{default}}}{{/if}};
{{/each}}

  // Component description: {{description}}
</script>

<svelte:options tag="{{kebab}}-embed" />

<div class="{{kebab}}-container">
  <!-- Legacy Svelte file-driven embed -->
  <slot />
</div>

<style>
  .{{kebab}}-container {
    width: 100%;
    height: 100%;
  }
</style>
